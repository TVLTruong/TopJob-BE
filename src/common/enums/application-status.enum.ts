export enum ApplicationStatus {
  NEW = 'NEW', // Ứng viên mới nộp đơn
  VIEWED = 'VIEWED', // Nhà tuyển dụng đã xem hồ sơ
  SHORTLISTED = 'SHORTLISTED', // Ứng viên được đưa vào danh sách "phù hợp"
  REJECTED = 'REJECTED', // Ứng viên bị từ chối
}
